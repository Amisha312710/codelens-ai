import React from 'react';

/**
 * Walkthrough Component
 * Video generation and scene playback walkthrough player.
 * Scheduled for full interactive implementation in Milestone 6.
 */
export default function Walkthrough({ scene }) {
  return (
    <div className="walkthrough-stage-card">
      <div style={{ color: 'var(--text-muted)', fontSize: '12px' }}>
        Walkthrough Player Component
      </div>
    </div>
  );
}
